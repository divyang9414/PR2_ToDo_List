<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        body{
            position: relative;
        }
        .bgrnd{
            background-image: url('/assets/audi_etron.jpg');
            background-repeat: no-repeat;
            background-size: cover;
        }
        .bgrnd::before{
            content: '';
            background-color: rgba(0, 0, 0, 0.650);
            position: absolute;
            top: 0;
            right: 0;
            bottom: 0;
            left: 0;
        }
        form input{
            outline: none;
        }
        .my_box{
            height: 100%;
            padding: 40px;
            overflow: hidden;
        }
        .task-list{
            height: 380px;
            overflow-y: scroll; 
        }
        .bdfilter{
            backdrop-filter: blur(3px);
        }
    </style>
</head>

<body>
    <div class="container-fluid py-5 vh-100 bgrnd">
        <div class="row my_box justify-content-center">
            <div class="col-10 rounded-4 bdfilter pb-5">
                <h1 class="text-center text-white mb-5">Divyang's To do list</h1>
                <form action="/insertData" method="post">
                    <input type="text" name="taskName" id="" class="form-control fs-4 mb-2" placeholder="Enter task name">
                    <input type="submit" name="" id="" value="Add task" class="bg-secondary py-2 fs-5 px-5 border-0 rounded-2">
                    <div class="form-content pt-5">
                        <div class="py-3 task-list">
                            <% givenTask.map((val)=>{ %>
                                <div class="col-12 d-flex mb-2 justify-content-between">
                                    <div class="info-Block d-flex">
                                        <h3 class="d-none"><%= val.id %></h3>
                                        <h3 class="text-white-50"><%= val.taskName %></h3>
                                    </div>
                                    <div class="a-tags me-2">
                                        <a href="/editTask?id=<%= val.id %>" class="fs-4 text-bg-secondary px-4 py-1 text-decoration-none rounded-3 me-2">Edit</a>
                                        <a href="/completeTask?id=<%= val.id %>" class="fs-4 text-bg-secondary px-4 py-1 text-decoration-none rounded-3">Complete</a>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

</html>